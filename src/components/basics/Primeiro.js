import React from 'react'

export default function Primeiro() {
    const msg = 'Exemplo de um componente React'

    return (
        <div>
            <h2>Primeiro Componente</h2>
            <h2>{msg}</h2>
        </div>
)
}